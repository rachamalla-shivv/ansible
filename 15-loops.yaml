 - name: install packages
   hosts: local
   connection: local
   tasks:
   - name: install package
     ansible.builtin.package:
        name: "{{ item }}"
        state: latest
     loop:
     - nginx
     - mysql
     - zip