- name: conditions
  hosts: local
  connection: local
  vars_prompt:
  - name: number
    prompt: number
    private: false
  tasks:
  - name: check whether the number is less than 10 
    ansible.builtin.debug:
     msg: "Given number {{ number }} is less than 10"
    when: number < 10